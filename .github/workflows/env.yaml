name: Practice Environments

on: [push]

jobs:
  test-env:
    environment:
      name: practice
      url: http://example.com
    runs-on: ubuntu-latest
    steps:
      - name: Echo Environment name
        run: echo "Environment => practice"
        shell: bash
